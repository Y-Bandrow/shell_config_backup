this repository is create for backup my fedora shell config

# useage
./restore.sh 1.backup into tar 2.restore update_fold into $HOME
./backup.sh backup $HOME config files into tar named by time
./update.sh update $HOME config files into update_fold

# ~/
clang-format
.ssh
.vimrc
.zshrc
.bashrc
.tmux.conf

# ~/.config
nvim
kitty
fcitx
fcitx5
